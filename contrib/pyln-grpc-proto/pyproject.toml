[project]
name = "pyln-grpc-proto"
version = "0.1.2"
description = "The compiled GRPC proto for CLN"
authors = [{ name = "Christian Decker", email = "decker@blockstream.com" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8,<4.0"
dependencies = ["grpcio", "protobuf==5.29.4"]

[dependency-groups]
dev = ["grpcio", "twine>=4.0.2", "mypy-protobuf<3.5.0", "grpcio-tools>=1.59.2"]

[tool.hatch.build.targets.wheel]
packages = ["pyln"]

[build-system]
requires = ["hatchling>=1.0.0"]
build-backend = "hatchling.build"
